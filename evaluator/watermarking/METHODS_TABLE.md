# Watermarking Methods Table

Updated: 2026-06-27

Scope: the first table covers the watermarking methods currently registered under `evaluator/watermarking/methods/`. Image-size and capacity notes describe the current local wrappers, not only the upstream papers. For variable-size methods, the listed capacity is the wrapper default or the packaged checkpoint capacity. Scratch-only methods that were reproduced or audited but not promoted are listed separately below the registered-method table.

| Registered method | Image input size in this workspace | Embedding capacity | GitHub repository | Paper link | Watermark characteristics and notes |
| --- | --- | --- | --- | --- | --- |
| `traditional-dct` | Original RGB size; needs at least one 8x8 block; output keeps original size. | Default 56 bits; max `floor(W/8) * floor(H/8)` bits. | Internal implementation | Classical DCT watermark family; no single upstream paper. | Weightless frequency-domain baseline; very fast; robustness mainly targets mild compression/noise, not geometry changes. |
| `traditional-spread-dct` | Original RGB size; 8x8 block grid; output keeps original size. | Default 56 bits; max `floor(W/8) * floor(H/8)` bits. | Internal implementation | Classical spread-spectrum DCT watermark family; no single upstream paper. | Weightless spread DCT baseline; uses repeated coefficient signs for a stronger bit decision than plain DCT. |
| `traditional-lsb` | Original RGB size; output keeps original size. | Default 56 bits; max `W * H` bits in the current blue-channel path. | Internal implementation | Classical LSB steganography; no single upstream paper. | Highest simple baseline capacity; fragile to JPEG, resize, denoising, and most image processing. |
| `traditional-haar` | Original RGB size; uses 2x2 blocks; output keeps original size. | Default 56 bits; max `floor(W/2) * floor(H/2)` bits. | Internal implementation | Classical Haar/wavelet watermark family; no single upstream paper. | Weightless block/wavelet-style baseline; faster and simpler than neural methods, but not a modern robust watermark. |
| `blind_watermark` | Original image path is passed to the packaged library; output is normally original size. | Variable string payload; wrapper computes `wm_shape` from the UTF-8 message and image capacity is content/size dependent. | [guofei9987/blind_watermark](https://github.com/guofei9987/blind_watermark) | Engineering library for DWT-DCT-SVD-style blind watermarking; no single official paper in the repo. | Blind extraction with `password_img` and `password_wm`; no model weights; useful classical robust baseline. |
| `invisible-watermark-dwtdct` | Original OpenCV BGR image size; output keeps original size. | Default 56 bits, i.e. 7 UTF-8 bytes in this wrapper. | [ShieldMnt/invisible-watermark](https://github.com/ShieldMnt/invisible-watermark) | DWT-DCT classical method; upstream repo does not bind it to a single paper. | Fast CPU-oriented DWT + DCT method; blind decode; generally weaker to resize/crop than neural methods. |
| `invisible-watermark-dwtdctsvd` | Original OpenCV BGR image size; output keeps original size. | Default 56 bits, i.e. 7 UTF-8 bytes in this wrapper. | [ShieldMnt/invisible-watermark](https://github.com/ShieldMnt/invisible-watermark) | DWT-DCT-SVD classical method; upstream repo does not bind it to a single paper. | DWT + DCT + SVD variant; usually slower than `dwtDct`; no production weights needed. |
| `invisible-watermark-rivagan` | Original OpenCV BGR image size; ONNX encoder/decoder; output keeps original size. | Fixed 32 bits. | [ShieldMnt/invisible-watermark](https://github.com/ShieldMnt/invisible-watermark) | [RivaGAN, arXiv:1909.01285](https://arxiv.org/abs/1909.01285) | Neural attention-based watermark variant packaged from the invisible-watermark repo; better crop behavior than the frequency variants in the upstream README. |
| `trustmark` | Arbitrary PIL RGB resolution; output keeps original size. | 100 bits. | [adobe/trustmark](https://github.com/adobe/trustmark) | [arXiv:2311.18297](https://arxiv.org/abs/2311.18297) and [ICCV 2025 PDF](https://collomosse.com/pubs/Bui-ICCV-2025.pdf) | Default wrapper uses compact model C; supports C2PA-friendly message/schema handling; local weights are packaged. |
| `trustmark-c` | Arbitrary PIL RGB resolution; output keeps original size. | 100 bits. | [adobe/trustmark](https://github.com/adobe/trustmark) | [arXiv:2311.18297](https://arxiv.org/abs/2311.18297) and [ICCV 2025 PDF](https://collomosse.com/pubs/Bui-ICCV-2025.pdf) | Compact TrustMark-C variant; small/fast neural watermark with packaged encoder and decoder. |
| `trustmark-q` | Arbitrary PIL RGB resolution; output keeps original size. | 100 bits. | [adobe/trustmark](https://github.com/adobe/trustmark) | [arXiv:2311.18297](https://arxiv.org/abs/2311.18297) and [ICCV 2025 PDF](https://collomosse.com/pubs/Bui-ICCV-2025.pdf) | Quality-oriented TrustMark-Q variant; same 100-bit payload with a heavier model than C. |
| `rawatermark` | Wrapper resizes input to 512x512; output is 512x512. | Zero-bit detection only; returns watermark presence/probability instead of a decoded message. | [jeremyxianx/RAWatermark](https://github.com/jeremyxianx/RAWatermark) | [arXiv:2403.18774](https://arxiv.org/abs/2403.18774) | Plug-and-play watermark and detector pair; model-agnostic marker with provable false-positive discussion in the paper. |
| `videoseal` | Original tensor size is passed to the model; output keeps the input tensor size in the wrapper. | 256 bits. | [facebookresearch/videoseal](https://github.com/facebookresearch/videoseal) | [VideoSeal, arXiv:2412.09492](https://arxiv.org/abs/2412.09492) | VideoSeal v1.0 image-mode checkpoint; efficient image/video watermark with JND-style attenuation in the model family. |
| `pixelseal` | Original tensor size is passed to the model; output keeps the input tensor size in the wrapper. | 256 bits. | [facebookresearch/videoseal](https://github.com/facebookresearch/videoseal) | [PixelSeal, arXiv:2512.16874](https://arxiv.org/abs/2512.16874) | VideoSeal-family adversarial-only model focused on robustness and imperceptibility; included as a required large-checkpoint exception. |
| `chunkyseal` | Original tensor size is passed to the model; output keeps the input tensor size in the wrapper. | 1024 bits. | [facebookresearch/videoseal](https://github.com/facebookresearch/videoseal) | [ChunkySeal, arXiv:2510.12812](https://arxiv.org/abs/2510.12812) | High-capacity VideoSeal-family model; largest payload in the current registry; included as a required size/speed exception. |
| `wam` | Original PIL RGB size; local `default_transform` has no resize; detector heatmap/message logits are 256x256. | 32 bits per localized watermark message in the packaged MIT checkpoint. | [facebookresearch/watermark-anything](https://github.com/facebookresearch/watermark-anything) | [arXiv:2411.07231](https://arxiv.org/abs/2411.07231) | Watermark Anything localized watermark; can detect mask-like watermark regions and recover the 32-bit message. |
| `maskwm-d32` | Resize to 512 then center crop to 512x512; output is 512x512. | 32 bits. | [hurunyi/MaskWM](https://github.com/hurunyi/MaskWM) | [arXiv:2504.12739](https://arxiv.org/abs/2504.12739) | Packaged `D_32bits` global MaskWM variant; mask-watermark family also has other upstream bit-depth variants not registered here. |
| `mbrs` | Resize to 256x256; output is 256x256. | 256 bits in the packaged `EC_42.pth` wrapper. | [jzyustc/MBRS](https://github.com/jzyustc/MBRS) | [arXiv:2108.08211](https://arxiv.org/abs/2108.08211) | Deep encoder-decoder watermark trained with mini-batches of real and simulated JPEG compression; robust JPEG-oriented baseline. |
| `cin` | Resize to 128x128; output is 128x128. | 30 bits. | [rmpku/CIN](https://github.com/rmpku/CIN) | [arXiv:2212.12678](https://arxiv.org/abs/2212.12678) and [ACM DL](https://dl.acm.org/doi/abs/10.1145/3503161.3547950) | Combines invertible and non-invertible mechanisms for blind watermarking under combined distortions. |
| `pimog` | Resize to 128x128; output is 128x128. | 30 bits. | [FangHanNUS/PIMoG](https://github.com/FangHanNUS/PIMoG-An-Effective-Screen-shooting-Noise-Layer-Simulation-for-Deep-Learning-Based-Watermarking-Netw) | [ACM DL DOI:10.1145/3503161.3548049](https://doi.org/10.1145/3503161.3548049) | Screen-shooting-robust watermark network; upstream workflow expects perspective correction for captured screens. |
| `invismark` | Original image input; encoder/decoder operate at 256x256 internally; wrapper resizes the residual back so output keeps original size. | 100 bits. | [microsoft/InvisMark](https://github.com/microsoft/InvisMark) | [arXiv:2411.07795](https://arxiv.org/abs/2411.07795) | AI-generated image provenance watermark; packaged with user-supplied `paper.ckpt`; supports exact 100-bit binary payloads or wrapper-generated provenance bits. |
| `hidden` | Resize to 128x128 from the packaged HiDDeN config; output is 128x128. | 30 bits. | [ando-khachatryan/HiDDeN](https://github.com/ando-khachatryan/HiDDeN) and original [jirenz/HiDDeN](https://github.com/jirenz/HiDDeN) | [arXiv:1807.09937](https://arxiv.org/abs/1807.09937) | HiDDeN encoder-decoder baseline with the packaged JPEG-compression checkpoint; no ECC in the local extraction path. |
| `ssl-watermarking` | Original PIL RGB size; upstream `default_transform` only tensorizes and normalizes; output keeps original size. | Default 30-bit multi-bit mode in this wrapper; upstream also supports 0-bit. | [facebookresearch/ssl_watermarking](https://github.com/facebookresearch/ssl_watermarking) | [arXiv:2112.09581](https://arxiv.org/abs/2112.09581) | Self-supervised latent-space watermarking using DINO ResNet50 features, normalization layer, and a packaged carrier; embedding is iterative optimization rather than a single encoder pass. |
| `stegastamp` | Center-crop/resize to 400x400 through the packaged inference API; output is 400x400. | 100 raw bits, or up to 7 ASCII characters with BCH/ECC in the packaged API. | [tancik/StegaStamp](https://github.com/tancik/StegaStamp) | [arXiv:1904.05343](https://arxiv.org/abs/1904.05343) | Physical-world watermark for print-and-camera recovery; local package uses PyTorch weights and can also save a residual visualization. |

## Scratch-Only / Not Promoted

| Method | Current workspace status | Image input size observed from upstream wrapper | Embedding capacity | GitHub repository | Paper link | Watermark characteristics and notes |
| --- | --- | --- | --- | --- | --- | --- |
| `vine` | Scratch source and decoder-only smoke are present under `算法/`; not registered in formal benchmark paths. | Upstream encoder crops non-square inputs to square, resizes to 256x256 for VINE encoding, then scales the residual back to the cropped/original square size; decoder resizes input to 256x256. | 100 bits, or up to 12 UTF-8 characters plus 4 padding bits in the upstream text path. | [Shilin-LU/VINE](https://github.com/Shilin-LU/VINE) | [arXiv:2410.18775](https://arxiv.org/abs/2410.18775) | VINE uses generative priors for robustness against image editing. Scratch audit confirmed `VINE-R-Dec` can load and emit 100-bit predictions, but full embed is blocked from promotion because the encoder hard-loads `stabilityai/sd-turbo` tokenizer, text encoder, UNet, VAE, and scheduler, plus a roughly 3.8 GB VINE encoder checkpoint. |

## Summary

- Capacity tiers: `chunkyseal` is the current high-capacity outlier at 1024 bits; `videoseal`, `pixelseal`, and `mbrs` sit at 256 bits; `trustmark*`, `invismark`, and `stegastamp` are 100-bit methods; `wam`, `maskwm-d32`, `hidden`, `cin`, `pimog`, and `ssl-watermarking` are 30 to 32-bit methods; `rawatermark` is zero-bit detection.
- Size behavior matters for fair benchmarks: `cin`, `pimog`, `hidden`, `mbrs`, `maskwm-d32`, `rawatermark`, and `stegastamp` produce fixed-size outputs, while the traditional methods, invisible-watermark variants, TrustMark, VideoSeal-family methods, WAM, SSL watermarking, and InvisMark keep or restore the original image size in the local wrappers.
- Classical/no-weight baselines are useful for speed and interpretability, but they are usually weaker against resizing, crop, and recompression than modern neural methods.
- Neural methods cover different use cases: TrustMark is compact and arbitrary-resolution; VideoSeal/PixelSeal favor robust image/video messages; ChunkySeal tests high payload capacity; WAM adds localization; RAWatermark is a fast detector-only watermark; StegaStamp targets physical print-camera recovery.
- Local packaging exceptions are intentional: `pixelseal`, `chunkyseal`, and `invismark` have large checkpoints, and `chunkyseal` is above the strict subsecond embed gate locally, but they are included because they are required benchmark algorithms.
- `vine` is documented as scratch-only rather than registered: it is a strong image-editing-robust method on paper, but the current project gate excludes methods that require Stable Diffusion, SDXL, or other diffusion model weights, and VINE's encoder depends on SD-Turbo at runtime.
