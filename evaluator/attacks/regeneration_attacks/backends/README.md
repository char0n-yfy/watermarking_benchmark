Regeneration attack backend sources live here so the evaluator package remains
self-contained at the source-code level.

Do not place backend source trees under `resources/weights/attacks/`. That
resource area should contain model checkpoints, adapter weights, tokenizer and
model config files, and other downloadable model assets only.

