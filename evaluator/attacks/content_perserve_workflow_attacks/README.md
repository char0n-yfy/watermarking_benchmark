# Content-Preserving Workflow Attacks

本目录预留给内容保持型流程攻击，例如先降质再恢复、编辑后重建、超分/去噪/压缩流水线等。

接口必须保持图片文件输入、图片文件输出：

```text
watermarked image path -> workflow-attacked image path
```

目录名沿用当前工程中的 `content_perserve_workflow_attacks`。后续如果要对外发布，可以考虑统一改为 `content_preserve_workflow_attacks`。
