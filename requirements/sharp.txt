# Optional CUDA-heavy dependencies for the SHARP 3D Viewpoint Re-rendering attack.
#
# These are installed only when WM_BENCH_INSTALL_SHARP_DEPS=1. They are kept
# separate because gsplat and CUDA-enabled torch wheels are not suitable for
# every local macOS/Windows development environment.
gsplat>=1.5.3 ; platform_system == "Linux"
imageio[ffmpeg]>=2.37.0
pillow-heif>=1.1.1
plyfile>=1.1.2
huggingface-hub>=0.23,<1
timm>=1.0.20
